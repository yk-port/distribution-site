import { AppBar, Toolbar } from "@material-ui/core";
import React, { FC } from "react";

const TopHeader: FC = () => {
  return (
    <AppBar>
      <Toolbar></Toolbar>
    </AppBar>
  );
};

export default TopHeader;
